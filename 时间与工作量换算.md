# RimWorld 工作量 · 游戏刻(Tick) · 真实时间换算手册

> 适用版本：**RimWorld 1.6**
> 编写目的：把「工作量（work amount）」「游戏刻（tick）」「真实时间」三者的换算一次讲透，
> 并以本 mod（愚公移山，默认工作量 `120000` tick）为实例给出可直接查的对照表。

---

## 0. 一句话结论（TL;DR）

- **60 tick = 1 真实秒**（游戏速度 1x 时）。
- **1 个游戏日 = 60,000 tick** = 1x 速度下 **16 分 40 秒**真实时间。
- **实际耗时(tick) = 工作量 ÷ 小人的工作速度**。本 mod 默认 120,000 tick，
  建造速度 1.0 的小人在 1x 速度下要凿 **33 分 20 秒**（= 2 个游戏日）。

---

## 1. 两个基本概念

### 1.1 游戏刻（Tick）

**Tick 是 RimWorld 逻辑的最小时间单位。** 游戏里一切推进——移动、工作、饥饿、
事件、植物生长——都按 tick 一拍一拍地走。

- 它和"帧（FPS）"不同：**tick 是逻辑节拍，帧是画面刷新**。画面卡不卡不影响 tick 的计数，
  逻辑始终按固定节拍推进。
- 引擎默认以 **每秒 60 tick**（60 TPS）运行，这是连接"tick"与"真实时间"的桥梁。
- 提高游戏速度（2x / 3x）本质上是**每秒跑更多 tick**。

### 1.2 工作量（Work Amount）

**工作量是"完成一件工作所需的劳动总量"，单位同样是 tick。**

- 原版的 `WorkToBuild`（建造）、`WorkToMake`（制作/配方）等 stat，数值单位都是 tick。
- 本 mod 沿用同一单位：Mod 设置里的"每格工作量" `workAmount`（默认 120,000）就是 tick。
- 工作量是**总量**；小人每秒实际能"消化"多少，取决于它的**工作速度 stat**（见第 4 节）。

> ⚠️ 本 mod 只针对**厚岩顶**（overhead mountain）。原版游戏中厚岩顶**不可**移除，
> 只有普通薄屋顶能被「移除屋顶」指令清除。本 mod 的工作量体系是自建的，与原版建筑相互独立。

---

## 2. 游戏刻 ↔ 真实时间

### 2.1 基准（1x 速度）

| 换算 | 数值 |
|---|---|
| **60 tick** | = 1 真实秒 |
| **1 tick** | ≈ 16.67 毫秒 |
| 3,600 tick | = 1 真实分钟 |

### 2.2 游戏速度倍率

提高速度 = 每秒跑更多 tick，因此**同样的 tick 数在更高速度下对应更短的真实时间**。

| 游戏内速度 | 倍率 | 每秒 tick | 1 tick 真实时长 |
|---|---|---|---|
| 暂停 | 0x | 0 | — |
| 正常 | **1x** | 60 | 16.67 ms |
| 快速 | **2x** | 120 | 8.33 ms |
| 超快 | **3x** | 180 | 5.56 ms |
| 极快（仅开发者模式） | 4x | 240 | 4.17 ms |

### 2.3 换算公式

```
真实秒数 = tick 数 ÷ 60 ÷ 速度倍率
```

例：120,000 tick 在 3x 速度下 = 120,000 ÷ 60 ÷ 3 ≈ 666.7 秒 ≈ 11 分 7 秒。

---

## 3. 游戏内时间单位

游戏内的"小时 / 天 / 季节 / 年"是用 tick 定义的固定常量，与真实时间的换算随速度而变。

### 3.1 单位定义（固定不变）

| 游戏内单位 | tick 数 | 说明 |
|---|---|---|
| 1 小时 | **2,500** | 一天 24 小时 |
| 1 天 | **60,000** | 24 小时 × 2,500 |
| 1 个季节（quadrum） | **900,000** | 15 天 |
| 1 年 | **3,600,000** | 4 个季节 = 60 天 |

### 3.2 换算成真实时间

| 游戏内单位 | tick | 1x | 2x | 3x |
|---|---|---|---|---|
| 1 小时 | 2,500 | 41.7 秒 | 20.8 秒 | 13.9 秒 |
| **1 天** | 60,000 | **16 分 40 秒** | 8 分 20 秒 | 5 分 33 秒 |
| 1 季 | 900,000 | 4 时 10 分 | 2 时 5 分 | 1 时 23 分 |
| 1 年 | 3,600,000 | 16 时 40 分 | 8 时 20 分 | 5 时 33 分 |

> 💡 记忆口诀：**"1 天 6 万刻，1x 下一刻一分六"** —— 60,000 tick/天，1x 下 1 tick≈16.67ms、
> 一天正好 1000 秒（16 分 40 秒）。

---

## 4. 工作量 → 实际耗时

这是最容易踩坑的一环：**工作量是总量，真实耗时还要除以小人的工作速度。**

### 4.1 核心公式

```
实际耗时(tick) = 工作量(tick) ÷ 小人每 tick 推进量
本 mod：每 tick 推进量 = 小人的 ConstructionSpeed（建造速度）
∴ 实际耗时(tick) = 工作量 ÷ ConstructionSpeed
```

### 4.2 工作速度（stat）从哪来

`ConstructionSpeed` 这类工作速度 stat 由多种因素综合决定：

- **技能等级**：建造（Construction）技能越高，速度越快（最主要因素）。
- **特性**：如"勤快"类特性提速、"懒惰"类降速。
- **健康状态**：意识（Consciousness）、操作能力（Manipulation）受损会直接拖慢。
- **装备 / 义体**：部分义体、工具柜（Tool Cabinet）等提供加成。
- **环境**：黑暗、极端温度等会施加惩罚。

一个**中等技能、无特殊加成的普通殖民者**，`ConstructionSpeed` 大致在 **1.0 上下**——
此时"工作量数字 ≈ 实际要花的 tick 数"，这也是本 mod 注释里"按速度 1.0 估算"的由来。

> 🔍 **怎么查某个小人的实际速度**：选中殖民者 → 角色面板（**i** 图标）→ 属性（Gear/Stats）→
> 找到「建造速度 ConstructionSpeed」，那里显示的就是综合计算后的真实数值，代入下表即可。

### 4.3 本 mod 的实现方式（无隐藏倍率）

本 mod 每 tick 直接累加 `ConstructionSpeed`，**没有任何额外的全局缩放常数**：

```csharp
// Source/MountainMoving/JobDriver_RemoveThickRoof.cs
float done = comp.GetProgress(cell);
done += actor.GetStatValue(StatDefOf.ConstructionSpeed);   // 每 tick 加这么多
comp.SetProgress(cell, done);
...
if (done < MountainMovingMod.WorkAmount) return;             // 攒够工作量才完成
```

好处是**所见即所得**：设置里填的工作量 = 速度 1.0 的小人真实要花的 tick 数，
换算完全透明，不需要再乘任何系数。

> ⚠️ **对比原版的一个细节**：原版某些工作（典型如建造建筑）在结算时，除了乘小人速度外
> 还会再乘一个全局常数（社区普遍认为约 **1.7**），因此原版 `WorkToBuild` 的字面数字往往
> **大于**实际耗时——"看着很多，干起来没那么久"。
> **请注意：本机未安装 RimWorld、无法反编译核实，这个 1.7 仅为经验参考值**；
> 若需精确值，请用 dnSpy 反编译原版 `JobDriver_ConstructFinishFrame`，
> 查看其中乘在 `ConstructionSpeed` 上的常量。**本 mod 不受影响（上面代码可见就是 1×）。**

### 4.4 进度可持续（中断不清零）

本 mod 把每格进度存进 `MapComponent`（见 `MapComponent_RoofRemoval.cs`），
所以**吃饭、睡觉、读档后进度保留**。上面算出的"实际耗时"可以理解为
"累计投入的有效工作时间"，可以分多次完成，不要求一口气凿完。

---

## 5. 本 mod 实例换算

### 5.1 设置滑块全范围（小人速度 1.0 时）

滑块范围 `6,000 ~ 600,000` tick，步进 6,000（= 0.1 天），默认 `120,000`（= 2 天）。
（见 `Source/MountainMoving/MountainMovingSettings.cs`）

| 设置值(tick) | 游戏日 | 1x | 2x | 3x |
|---|---|---|---|---|
| 6,000（最小） | 0.1 | 1 分 40 秒 | 50 秒 | 33 秒 |
| 30,000 | 0.5 | 8 分 20 秒 | 4 分 10 秒 | 2 分 47 秒 |
| 60,000 | 1 | 16 分 40 秒 | 8 分 20 秒 | 5 分 33 秒 |
| **120,000（默认）** | **2** | **33 分 20 秒** | 16 分 40 秒 | 11 分 7 秒 |
| 300,000 | 5 | 1 时 23 分 | 41 分 40 秒 | 27 分 47 秒 |
| 600,000（最大） | 10 | 2 时 47 分 | 1 时 23 分 | 55 分 33 秒 |

### 5.2 默认 120,000 tick：不同建造速度下的耗时

把角色面板里查到的 `ConstructionSpeed` 代进来即可（数值越高手越快、耗时越短）：

| ConstructionSpeed | 实际 tick | 1x | 2x | 3x |
|---|---|---|---|---|
| 0.5（很低） | 240,000 | 66 分 40 秒 | 33 分 20 秒 | 22 分 13 秒 |
| 0.75 | 160,000 | 44 分 27 秒 | 22 分 13 秒 | 14 分 49 秒 |
| **1.0（普通）** | **120,000** | **33 分 20 秒** | 16 分 40 秒 | 11 分 7 秒 |
| 1.25 | 96,000 | 26 分 40 秒 | 13 分 20 秒 | 8 分 53 秒 |
| 1.5 | 80,000 | 22 分 13 秒 | 11 分 7 秒 | 7 分 24 秒 |
| 2.0（很高） | 60,000 | 16 分 40 秒 | 8 分 20 秒 | 5 分 33 秒 |

> 📌 调平衡建议：想让"一个普通小人在 1x 下凿一格约 X 分钟"，就把工作量设为
> `X × 60 × 60 = X × 3600` tick（因为 1 分钟 = 60 秒 × 60 tick/秒 = 3,600 tick）。
> 例如想要 10 分钟 → 36,000 tick（= 0.6 天）。

---

## 6. 自己动手查证原版数值

本机没有 RimWorld、无法直接翻原版数据时，可在游戏内用开发者模式查任意东西的权威数值：

1. **查建筑/物品的工作量**：选项 → 开启"开发者模式"（Development mode）→
   选中地图上的建筑/物品 → 点信息卡（**i**）→ 查看 `WorkToBuild` / `WorkToMake`（单位即 tick）。
   建造菜单里悬停某个蓝图，也会显示"工作"量。
2. **查小人的工作速度**：选中殖民者 → 角色面板（i）→ 属性 →
   看 `ConstructionSpeed`、`MiningSpeed`、`PlantWorkSpeed` 等的实时综合值。
3. **查反编译源码**：用 dnSpy 打开
   `<RimWorld>/RimWorldWin64_Data/Managed/Assembly-CSharp.dll`，搜索
   `GenDate`（时间常量）、`JobDriver_ConstructFinishFrame`（建造结算）、`StatDefOf` 等。

---

## 7. 公式 & 常量速查卡

### 时间常量

```
1 tick        = 逻辑最小单位
60 tick       = 1 真实秒（1x）
2,500 tick    = 1 游戏小时
60,000 tick   = 1 游戏日
900,000 tick  = 1 个季节（15 天）
3,600,000 tick= 1 年（4 季 = 60 天）
```

### 换算公式

```
真实秒数   = tick ÷ 60 ÷ 速度倍率(1/2/3)
实际耗时   = 工作量 ÷ 工作速度(如 ConstructionSpeed)
───────── 合并 ─────────
真实秒数   = 工作量 ÷ 工作速度 ÷ 60 ÷ 速度倍率
反推工作量 = 想要的真实秒数 × 60 × 速度倍率 × 工作速度
```

### 本 mod 默认值

```
默认工作量 workAmount = 120,000 tick = 2 游戏日
  速度 1.0 小人 @1x ≈ 33 分 20 秒
滑块范围 6,000 ~ 600,000 tick（0.1 ~ 10 游戏日），步进 6,000
每 tick 进度 += 小人的 ConstructionSpeed（无额外倍率）
进度按格存 MapComponent，中断/读档可续作
```

---

*本手册数值基于 RimWorld 1.6 的通用时间常量与本 mod 源码；唯一未能在本机核实的是
第 4.3 节提到的原版建造结算常数（约 1.7），已就地标注，请以反编译原版源码为准。*
